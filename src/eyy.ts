/**
 * eyy
 * eyyeyyeyy...
 */


function e(n: number): string {
  return 'e'.repeat(n)
}

function y(n: number): string {
  return 'y'.repeat(n)
}

function ey(n: number): string {
  return 'ey'.repeat(n)
}

function eyy(n: number): string {
  return 'eyy'.repeat(n)
}


function yy(n: number): string {
  return 'yy'.repeat(n)
}

/**
 * TODO: document
 */
export default {
  e,
  y,
  ey,
  eyy,
  yy
}
